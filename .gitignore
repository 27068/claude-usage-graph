node_modules/
out/
*.vsix
.vscode-test/

# esbuild bundle of src/webview/client/. Regenerated by `npm run compile`, which
# every F5 configuration runs as its preLaunchTask and `vscode:prepublish` runs
# before packaging — so nothing needs the checked-in copy.
media/dashboard.js

# Isolated user-data-dir for the screenshot launch configuration
.vscode-screenshot-host/

# Machine-specific developer notes (see docs/DEVELOPING.md section 9)
docs/LOCAL.md

# Regenerated per screenshot session (scripts/make-screenshot-fixture.mjs)
fixtures/screenshot-usage.json
.vscode-test-host/
